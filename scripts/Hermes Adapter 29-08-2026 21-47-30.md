# Hermes Runtime Adapter

## Use

Supply this adapter together with `Master Prompt 29-08-2026 20-06-40.md`. The Master Prompt is the canonical publishing procedure. This adapter applies only to runtime discovery and fallback behavior for the particular Hermes deployment in use.

## Instructions to Hermes

1. Read the complete Master Prompt before starting.
2. Identify the exact Hermes product, host, model, execution environment, and tool layer. The name `Hermes` alone does not establish filesystem, terminal, browser, GitHub, memory, or deployment capabilities.
3. Inventory visible tools and safely verify uploaded-file, filesystem, terminal, package, Git, authenticated GitHub, browser, local-preview, and deployment-monitoring access.
4. Complete the Master Prompt's `runtime_capabilities` block using `verified`, `unavailable`, `restricted`, or `unknown`, with concise evidence.
5. Treat custom tools, MCP servers, plugins, memories, and system integrations as unavailable until they are visible and callable in the current session.
6. Do not claim persistent file changes, external actions, tests, commits, pushes, deployments, or live verification without observable results.
7. If the deployment is chat-only or lacks required tools, produce portable UTF-8 artifacts, deterministic scripts, validation commands, and a human-executable handoff checklist; mark unexecuted work `unverified`.
8. Do not embed credentials or private legal material in prompts, adapters, scripts, logs, or repositories.
9. Preserve the Master Prompt's authority boundaries and stop conditions even if the runtime supports autonomous execution.
10. Keep Hermes-specific tool names and invocation syntax in this adapter or a deployment-local configuration, never in the canonical Master Prompt.

Begin with the runtime-capability declaration, then proceed within the verified scope.
